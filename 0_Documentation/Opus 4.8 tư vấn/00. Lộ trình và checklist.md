# 00. Lộ trình và checklist

Tệp này không phải nguồn chuẩn về thiết kế — nó chỉ trả lời ba câu: **đang ở đâu**, **còn treo cái gì**, **làm gì tiếp**. Cập nhật lần cuối 04/09/2026.

## Phần 1 — Đang ở đâu

| Giai đoạn | Nội dung | Trạng thái |
|---|---|---|
| Thiết kế lõi | Tài liệu 01 → 08 cùng bốn hợp đồng | ✅ xong, đã dọn hết mâu thuẫn |
| Thiết kế đồng bộ FBM, phiên 5 | `09. Đồng bộ FBM.md` và `09A. Hợp đồng đồng bộ FBM.md` | ✅ chốt 04/09/2026, đã áp hết bản vá vào các tài liệu khác |
| **Phiên SOP, phiên thiết kế cuối** | Sheet `Log`, kênh báo lỗi, module ghi log dùng chung, gom TODO tương lai | ⬜ **đang tới** |
| Viết code lõi | Chưa bắt đầu một dòng nào theo kiến trúc mới | ⬜ |
| Chuyển sheet thật sang tên mới | Sheet đang chạy còn tên cũ `ListKH`, `Act`, `Help`, `QL_System` | ⬜ |
| Dựng module `fbm_sync` | Kiểm chứng thực nghiệm, nạp lần đầu, nhập hàng loạt, dữ liệu bẩn của FBM | ⬜ |

Việc dài nhất còn lại là **viết code lõi**, không phải viết tài liệu. Sau phiên SOP thì bộ tài liệu đủ chi tiết để code mà không phải quyết thêm gì.

## Phần 2 — Tình trạng từng tài liệu

| Tài liệu | Tình trạng |
|---|---|
| 01 Quy chuẩn nền | Chuẩn. Bản đồ tài liệu ở Phần 5 đã cập nhật cho 09 và 09A |
| 02 Kiến trúc dữ liệu + 02A | Chuẩn. Vừa sửa: sheet `Log` nêu đúng tên, lớp kiểm thứ hai của cột đi kèm, mười cột / tám cột |
| 03 Schema + 03A | Chuẩn. Vừa sửa: `SYNC_SCHEMA` giữ `AliasName` và **không** giữ index cột grid, `normalize` chỉ chạy ở nguồn `user` |
| 04 Render và luồng lưu | Chuẩn. Vừa sửa: luồng lưu năm trạm theo luật mới, đường dẫn tệp thống nhất tiền tố `client/` |
| 05 Nạp dữ liệu và RAM + 05A | Chuẩn, không phải sửa gì |
| 06 Cửa ghi | Chuẩn. Vừa sửa: bảng nguồn × ca ba dòng, `pull` không chạy `normalize`, cửa ghi trả danh sách trường trượt |
| 07 Làm mới và sheet quản trị | Chuẩn, không phải sửa gì |
| 08 Ngôn ngữ lọc + 08A | Chuẩn, không phải sửa gì |
| 09 Đồng bộ FBM + 09A | Nguồn chuẩn của cơ chế đồng bộ, chốt 04/09/2026 |
| Biên bản phiên đồng bộ (26/08) | Bản nháp của tài liệu 09, nay hết vai. **Xóa** |
| Biên bản A (19/08) | Lịch sử quyết định của cả bộ 01–08, không phải nguồn chuẩn. **Giữ** — nó là thứ duy nhất ghi vì sao bộ tài liệu có hình dạng hiện tại |

## Phần 3 — Checklist việc phải làm, theo thứ tự

Nguyên tắc xếp thứ tự: **lõi trước, đồng bộ sau**. Lý do không phải sở thích mà là cấu trúc — tài liệu 01 tới 08 không phụ thuộc `fbm_sync` một dòng nào, nên bàn làm việc tra cứu và nhập liệu chạy được ngay, còn module đồng bộ ghép vào sau mà không phải sửa gì trong lõi. Làm ngược lại thì phải đợi xong toàn bộ phần khó nhất mới có cái để dùng.

### Giai đoạn 0 — Đóng bộ tài liệu

- [ ] Xóa biên bản 26/08. Nội dung đã lên tài liệu 09, lý do từng bản vá nằm ở commit `03bb51c`
- [ ] **Phiên SOP — phiên thiết kế cuối.** Bốn việc: hình dạng cột cuối cùng của sheet `Log` (lõi cũng cần log, không riêng `fbm_sync`); kênh hiển thị lỗi cho phần A và phần B, theo ràng buộc kế thừa ở 08A — `getUi()` không dùng được trong trigger, `toast` dùng được trong installable `onEdit`, trigger theo thời gian thì không có kênh nào nên lỗi phải cất lại và hiện ở lần tương tác kế tiếp; module báo lỗi và ghi log dùng chung, một tệp mọi nơi gọi tới, **không phải** Global Error Handler kiểu nuốt lỗi; gom các TODO tương lai rải trong bộ tài liệu về một chỗ

### Giai đoạn 1 — Dựng lõi, năm chặng, mỗi chặng chạy thử được ngay

| Chặng | Tệp | Chạy thử được gì |
|---|---|---|
| 1.1 Đọc được sheet | `server/Settings.gs`, `server/DataSchema.gs`, `server/SheetIo.gs` | Gọi một hàm trong editor, in ra bảng mã cột → chỉ số cột của `Customer`, sai một mã là báo lỗi ngay |
| 1.2 Nạp được vào RAM | `server/LoadService.gs`, `client/ram/store.js`, `client/ram/ingest.js` | Mở sidebar rỗng, xem log thấy đủ số bản ghi và thời gian nạp, đo ngân sách ô |
| 1.3 Vẽ được form | `client/schema/*`, `client/ui/*`, `Sidebar.html` | Bốn màn hiện đúng, bấm ô trên sheet thì form nhảy sang khách đó, chưa lưu được |
| 1.4 Lưu được | `server/FieldLogic.gs`, `server/WriteGate.gs`, `server/IdGate.gs`, `server/DeleteGate.gs`, `client/save/*` | Sửa một trường và lưu thật, thiếu trường bắt buộc thì ô bôi đỏ theo danh sách cửa ghi trả về, xóa mềm và hoàn tác |
| 1.5 Làm mới và sheet quản trị | `server/DirtyState.gs`, `server/ViewSheetRenderer.gs`, `server/Triggers.gs`, `client/ram/refresh.js` | Gõ điều kiện lọc trên sheet `!` và thấy nó in ra đúng, sửa trên sheet thì sidebar nạp lại đúng phần cần |

Xong chặng 1.4 là **đã dùng được để đi bán hàng**. Chặng 1.5 là tiện nghi, không phải điều kiện để dùng.

### Giai đoạn 2 — Chuyển sheet thật sang tên mới

- [ ] Sao lưu tệp Sheet, làm mọi việc trên **bản sao**, bản đang chạy không chạm
- [ ] Đổi tên sheet: `ListKH` → `Customer`, `Act` → `Activity`, `Help` → `Category`, `QL_System` → `Config`
- [ ] Viết lại hàng 1: `@KH_*` → `@CUS_*`, các mã còn lại theo tài liệu 02
- [ ] Thêm mười cột mô tả bản ghi, để trống, và sheet `Log`
- [ ] Chạy hàm tự kiểm của chặng 1.1 trên bản sao cho tới khi không còn lỗi mã cột
- [ ] Chỉ khi lõi chạy ổn trên bản sao thì mới chuyển sang dùng thật

### Giai đoạn 3 — Dựng module `fbm_sync`

Thứ tự trong giai đoạn này **không đổi được**: lượt kiểm chứng đứng trước, vì kết quả của nó có quyền phản bác thiết kế ở tài liệu 09, và biết trước thì không phải viết code trên giả định.

- [ ] Bảy mục kiểm chứng thực nghiệm ở tài liệu 09 Phần 13, làm trên một tài khoản FBM thật, chỉ đọc trừ khi buộc phải ghi, ca phải ghi thì dùng khách nháp tự tạo
- [ ] **Nạp mã danh mục vào sheet `Category` bằng cách bắn `GetCompletionList` lên FBM thật**, không chép từ tài liệu nghiên cứu FBM. Mã trong tài liệu nghiên cứu là ảnh chụp từ lúc viết chương 11, có thể đã lỗi thời — mà module vốn đã có sẵn lời gọi này trong thủ tục mở phiên, nên dùng nó là dùng đúng nguồn sự thật. Việc này làm cùng lượt với mục trên vì cả hai đều cần một tài khoản FBM đang đăng nhập
- [ ] Dựng `fbm_sync` phía GAS: `SYNC_SCHEMA`, dấu vân tay, so ba chiều
- [ ] Dựng Extension: chọn **một** nơi duy nhất làm nơi thật, `chrome.alarms`, content script gọi FBM, `doPost` với khóa dùng chung
- [ ] Chốt và viết ba việc ở loại một của tài liệu 09 Phụ lục B: lượt nạp lần đầu 1.700 khách, nhập hàng loạt lên FBM, xử lý dữ liệu bẩn của FBM

## Phần 4 — Bốn cái bẫy đã thấy trước

| Bẫy | Vì sao nó là bẫy | Cách tránh |
|---|---|---|
| Apps Script **không có** `import` | Mọi tệp `.gs` dùng chung một không gian tên toàn cục, nên luật "lõi không được đọc `fbm_sync`" là kỷ luật chứ không phải cơ chế; máy không chặn hộ | Cho toàn bộ module sống trong đúng một đối tượng toàn cục `FbmSync`, và có một hàm tự kiểm quét các tệp lõi tìm chuỗi `FbmSync` hoặc `SYNC_SCHEMA`, thấy là báo lỗi |
| Hai bản Extension | Bản khung trong `2_ShinCRM_Extension/` và bản chạy thật ở `D:\Program\0. Extension\MiniCRM_GoogleSheet` là hai thứ khác nhau, nuôi cả hai thì sẽ có ngày sửa bản không chạy | Chọn một nơi ngay ở đầu giai đoạn 3, nơi kia xóa hoặc đổi tên thành `_cu` |
| Code GAS cũ trong `1_ShinCRM_GAS/` | Nó **đang chạy được**, nên rất dễ vừa viết mới vừa vá cũ, và cuối cùng có hai hệ nửa vời | Không sửa nó nữa. Đọc để tham chiếu, còn code mới viết vào dự án Apps Script khác trên bản sao Sheet, tới lúc đủ tốt thì đổi sang dùng bản mới |
| Trần thời gian sáu phút của Apps Script | Không dính lõi, nhưng dính mọi thứ của `fbm_sync` | Đã có luật cắt lát ở tài liệu 09: một lát là một lời gọi cửa ghi, con trỏ tiến độ ở `DocumentProperties`, không có hàm "đồng bộ tất" |


